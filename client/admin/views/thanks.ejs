<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Thank You</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    body {
      background-color: #f8f9fc;
      padding-top: 20px;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
    }
    .card {
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .card-header {
      background-color: #28a745; /* Green for success */
      color: white;
      border-radius: 10px 10px 0 0;
    }
    .card-body {
      text-align: center;
      padding: 40px;
    }
    .btn-back {
      width: 100%;
      padding: 10px;
      font-size: 1.1rem;
      background-color: #4e73df;
      border-color: #4e73df;
    }
    .btn-back:hover {
      background-color: #3757b9;
      border-color: #3757b9;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <div class="card-header">
        <h5 class="mb-0">Thank You</h5>
      </div>
      <div class="card-body">
        <p class="lead">Thanks for reporting it! It will be sent to the required team.</p>
        <a href="/?objID=<%= query.objID %>" class="btn btn-back text-white mt-4">Report Another Event</a>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS and Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>